# short-dashboard - dependencies
#
# Core dashboard intentionally uses ONLY the Python standard library
# (urllib, json, smtplib, ssl, logging, datetime, calendar, email).
# No third-party packages needed for the main dashboard or fmp_client.py.
#
# If a third-party package is ever added, pin it here with == e.g.:
#   requests==2.32.3
#
# FMP API key: set FMP_API_KEY in .env (never commit .env)
# See fmp_client.py for usage.
